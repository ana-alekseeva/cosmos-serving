# Python / tooling
.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.DS_Store

# Downloaded / generated run outputs. `results/` is the download target for job2 & job3;
# keep the dir (results/.gitkeep) but ignore everything pulled into it.
results/*
!results/.gitkeep

# Real DROID captures (policy/capture.py): bulky image tensors, regenerable.
data/replay_real/

# Local secrets (.env). .env.example is the committed template.
.env

# Project-local editable checkout of the Nebius workbench (deploy/install_npa.sh).
.nebius-physical-ai/
